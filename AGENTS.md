# 项目协作规则

## Git 提交约定

- 每完成一个独立、可验证的功能，就创建一次单独的 Git commit。
- 不要把多个无关功能、重构和文档修改混入同一个 commit。
- 如果一项工作包含多个可独立理解的功能，应按功能拆分实现、验证和提交。
- 提交前必须检查 `git diff`、运行与改动相称的测试，并确认没有密钥、本机私密配置或其他敏感数据被纳入提交。
- 提交信息应简洁说明本次完成的功能，优先使用 `feat:`、`fix:`、`docs:`、`refactor:`、`test:`、`chore:` 等类型前缀。
- 每次提交后向用户报告提交号、提交内容和验证结果。
- 创建 commit 不等于自动推送；只有用户明确要求推送，或当前任务已经明确包含推送时，才执行 `git push`。
- 如果工作尚未形成一个完整、可验证的功能，不要为了制造提交而提交半成品。

## 面向初学者的说明

- 执行 Git 操作时，用简短中文说明当前步骤及其目的，帮助用户理解工作流。
- 遇到需要用户记忆的操作，优先固化到仓库文档、配置示例或一键脚本中，不依赖聊天记忆。
